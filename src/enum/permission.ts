export enum Action {
    CREATE = "create",
    READ = "read",
    UPDATE = "update",
    DELETE = "delete",
}

export enum Resource {
    ALL = "*",
    USER = "user",
    ROLE = "role",
    POEM = "poem",
    POEM_TYPES = "poemType",
    STORY = "story",
    HEMISTICH = "hemistich",
    COMMENT = "comment",
}
